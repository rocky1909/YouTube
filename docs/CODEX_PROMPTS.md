# Codex Prompt Pack

## Phase 1
`Set up auth with Supabase (email magic link), protect /dashboard, and create workspace on first login.`

## Phase 2
`Add project creation and persistence using the supplied supabase/schema.sql tables. Save brief fields and list projects by workspace.`

## Phase 3
`Replace mock prompt/story agents with OpenAI-backed adapters. Keep route contracts unchanged and return typed payloads only.`

## Phase 4
`Add async queue flow for image/voice/video agent tasks with status polling endpoints.`

## Phase 5
`Add team review flow with comments, approvals, and export readiness checks before publish.`
